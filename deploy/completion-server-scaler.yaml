apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: completion-server-scaler
spec:
  maxReplicas: 6
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: completion-server
  targetCPUUtilizationPercentage: 80
